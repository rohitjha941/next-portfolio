import styled from "styled-components";

export const Text = styled.div`
  text-align: left;
  font-size: 1.5rem;
  line-height: 1.875rem;
  letter-spacing: 0;
  color: var(--gray);
  opacity: 1;
`;

export const BlogsStructure = styled.div`
    margin: 0 18.75rem;
    background: var(--background-color-2) 0% 0% no-repeat padding-box;
    opacity: 1;
    padding: 7.625rem 11.25rem;
`;
